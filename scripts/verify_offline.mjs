import {readFile} from 'node:fs/promises';import {join} from 'node:path';
const required=['pyodide/pyodide.js','pyodide/pyodide.asm.js','pyodide/pyodide.asm.wasm','pyodide/pyodide-lock.json','pyodide/python_stdlib.zip','runtime-worker.js','runtime_bundle_b223.tgz','manifest.webmanifest','apple-touch-icon.png','icon-192.png','icon-512.png'];const sw=await readFile(join('dist','sw.js'),'utf8'),worker=await readFile(join('dist','runtime-worker.js'),'utf8'),manifest=JSON.parse(await readFile(join('dist','manifest.webmanifest'),'utf8'));for(const asset of required)if(!sw.includes(asset))throw new Error(`offline precache missing: ${asset}`);if(/https?:\/\//.test(worker))throw new Error('runtime worker still contains an external URL');if(manifest.display!=='standalone'||manifest.orientation!=='portrait'||manifest.start_url!=='/nobu-shinsen-app/')throw new Error('PWA manifest is not configured for iPhone standalone launch');for(const size of ['192x192','512x512'])if(!manifest.icons?.some(icon=>icon.sizes===size&&icon.type==='image/png'))throw new Error(`PWA manifest missing PNG icon: ${size}`);console.log(`offline precache verified: ${required.length} required assets`);
