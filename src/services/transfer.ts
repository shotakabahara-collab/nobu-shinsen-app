import {exportSchema,type CompanionExport,type Formation} from '../domain/schemas';export function createExport(formations:Formation[]):CompanionExport{return exportSchema.parse({schemaVersion:1,exportedAt:new Date().toISOString(),formations});}export function parseImport(input:string):CompanionExport{return exportSchema.parse(JSON.parse(input) as unknown);}export function downloadExport(value:CompanionExport){const url=URL.createObjectURL(new Blob([JSON.stringify(value,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`nobu-companion-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url);}
