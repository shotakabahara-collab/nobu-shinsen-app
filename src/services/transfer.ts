import {exportSchema,type BattleResult,type CompanionExport,type Formation,type SkillRecord,type WarriorRecord} from '../domain/schemas';
export function createExport(formations:Formation[],warriors:WarriorRecord[],skills:SkillRecord[],battleResults:BattleResult[]):CompanionExport{return exportSchema.parse({schemaVersion:2,exportedAt:new Date().toISOString(),formations,warriors,skills,battleResults});}
export function parseImport(input:string):CompanionExport{return exportSchema.parse(JSON.parse(input) as unknown);}
export function downloadExport(value:CompanionExport){const url=URL.createObjectURL(new Blob([JSON.stringify(value,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download=`nobu-companion-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url);}
