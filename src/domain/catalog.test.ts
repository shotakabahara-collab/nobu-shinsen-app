import {describe,expect,it} from 'vitest';import {skillRecordSchema,warriorRecordSchema} from './schemas';const base={id:crypto.randomUUID(),createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};describe('catalog schemas',()=>{it('validates warrior records',()=>expect(warriorRecordSchema.parse({...base,name:'黒田官兵衛'}).notes).toBe(''));it('rejects unknown skill categories',()=>expect(skillRecordSchema.safeParse({...base,name:'七十二の計',category:'不明'}).success).toBe(false));});
