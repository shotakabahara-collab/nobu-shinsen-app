import {create} from 'zustand';import {db} from '../db/database';import type {Formation} from '../domain/schemas';type State={formations:Formation[];loading:boolean;error:string|null;load:()=>Promise<void>;save:(v:Formation)=>Promise<void>;remove:(id:string)=>Promise<void>;replaceAll:(v:Formation[])=>Promise<void>};export const useAppStore=create<State>((set,get)=>({formations:[],loading:false,error:null,load:async()=>{set({loading:true,error:null});try{set({formations:await db.formations.orderBy('updatedAt').reverse().toArray(),loading:false});}catch(e){set({error:e instanceof Error?e.message:'読込に失敗しました',loading:false});}},save:async(v)=>{await db.formations.put(v);await get().load();},remove:async(id)=>{await db.formations.delete(id);await get().load();},replaceAll:async(v)=>{await db.transaction('rw',db.formations,async()=>{await db.formations.clear();await db.formations.bulkPut(v);});await get().load();}}));
