import Dexie,{type EntityTable} from 'dexie';import type {BattleResult,Formation,SkillRecord,WarriorRecord} from '../domain/schemas';type Tables={formations:EntityTable<Formation,'id'>;warriors:EntityTable<WarriorRecord,'id'>;skills:EntityTable<SkillRecord,'id'>;battleResults:EntityTable<BattleResult,'id'>};export const db=new Dexie('nobu-companion') as Dexie&Tables;db.version(1).stores({formations:'id,kind,name,updatedAt'});db.version(2).stores({formations:'id,kind,name,updatedAt',warriors:'id,&name,updatedAt',skills:'id,&name,category,updatedAt'});db.version(3).stores({formations:'id,kind,name,updatedAt',warriors:'id,&name,updatedAt',skills:'id,&name,category,updatedAt',battleResults:'id,allyId,enemyId,createdAt,status'});
