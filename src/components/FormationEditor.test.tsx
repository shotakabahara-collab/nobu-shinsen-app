import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it,vi} from 'vitest';import {FormationEditor} from './FormationEditor';describe('FormationEditor',()=>{it('does not save an incomplete formation',()=>{const save=vi.fn();render(<FormationEditor onSave={save} onCancel={()=>{}}/>);fireEvent.click(screen.getByRole('button',{name:'保存'}));expect(save).not.toHaveBeenCalled();expect(screen.getByRole('alert')).toBeVisible();});});
