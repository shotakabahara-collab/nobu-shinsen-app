import {useState} from 'react';import {formationSchema,troopTypes,type Formation,type SkillRecord,type WarriorRecord} from '../domain/schemas';import {Button} from './ui/button';
const roles=['大将','副将1','副将2'] as const;
export function createEmptyFormation(kind:'ally'|'enemy'='ally'):Formation{const now=new Date().toISOString();const warrior=()=>({id:crypto.randomUUID(),name:'',limitBreak:0,inherentSkill:'未登録',equippedSkills:['',''] as [string,string]});return {id:crypto.randomUUID(),name:'',kind,troopType:'騎馬',troopLevel:10,troops:10000,warriors:[warrior(),warrior(),warrior()],createdAt:now,updatedAt:now};}
export function FormationEditor({initial,warriors=[],skills=[],onSave,onCancel}:{initial?:Formation;warriors?:WarriorRecord[];skills?:SkillRecord[];onSave:(value:Formation)=>Promise<void>|void;onCancel:()=>void}){const [value,setValue]=useState<Formation>(()=>structuredClone(initial??createEmptyFormation()));const [error,setError]=useState('');const field='w-full rounded-lg border border-slate-600 bg-slate-950 p-3 text-white';function updateWarrior(index:number,patch:Partial<Formation['warriors'][number]>){const next=structuredClone(value.warriors);const current=next[index];if(!current)return;next[index]={...current,...patch};setValue({...value,warriors:next});}async function submit(e:React.FormEvent){e.preventDefault();const parsed=formationSchema.safeParse({...value,updatedAt:new Date().toISOString()});if(!parsed.success){setError(parsed.error.issues[0]?.message||'入力を確認してください');return;}await onSave(parsed.data);}
return <form onSubmit={e=>void submit(e)} className="space-y-4"><datalist id="warrior-catalog">{warriors.map(x=><option key={x.id} value={x.name}/>)}</datalist><datalist id="skill-catalog">{skills.map(x=><option key={x.id} value={x.name}/>)}</datalist><section className="rounded-2xl border border-slate-700 bg-slate-900 p-4"><h2 className="mb-4 text-lg font-bold">編成編集</h2><div className="grid grid-cols-2 gap-3"><label className="col-span-2 text-sm text-slate-400">編成名<input aria-label="編成名" className={field} value={value.name} onChange={e=>setValue({...value,name:e.target.value})}/></label><label className="text-sm text-slate-400">区分<select aria-label="区分" className={field} value={value.kind} onChange={e=>setValue({...value,kind:e.target.value as 'ally'|'enemy'})}><option value="ally">自軍</option><option value="enemy">敵軍</option></select></label><label className="text-sm text-slate-400">兵種<select aria-label="兵種" className={field} value={value.troopType} onChange={e=>setValue({...value,troopType:e.target.value as Formation['troopType']})}>{troopTypes.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm text-slate-400">兵種Lv<input aria-label="兵種Lv" className={field} type="number" min="1" max="10" value={value.troopLevel} onChange={e=>setValue({...value,troopLevel:Number(e.target.value)})}/></label><label className="text-sm text-slate-400">兵力<input aria-label="兵力" className={field} type="number" min="1" value={value.troops} onChange={e=>setValue({...value,troops:Number(e.target.value)})}/></label></div></section>{value.warriors.map((w,i)=><section key={w.id} className="rounded-2xl border border-slate-700 bg-slate-900 p-4"><h3 className="mb-3 font-bold text-amber-400">{roles[i]}</h3><div className="grid grid-cols-2 gap-3"><label className="col-span-2 text-sm text-slate-400">武将名<input list="warrior-catalog" aria-label={`${roles[i]} 武将名`} className={field} value={w.name} onChange={e=>updateWarrior(i,{name:e.target.value})}/></label><label className="text-sm text-slate-400">凸<input aria-label={`${roles[i]} 凸`} className={field} type="number" min="0" max="5" value={w.limitBreak} onChange={e=>updateWarrior(i,{limitBreak:Number(e.target.value)})}/></label><label className="text-sm text-slate-400">固有戦法<input list="skill-catalog" aria-label={`${roles[i]} 固有戦法`} className={field} value={w.inherentSkill} onChange={e=>updateWarrior(i,{inherentSkill:e.target.value})}/></label><label className="text-sm text-slate-400">装着戦法1<input list="skill-catalog" aria-label={`${roles[i]} 装着戦法1`} className={field} value={w.equippedSkills[0]} onChange={e=>updateWarrior(i,{equippedSkills:[e.target.value,w.equippedSkills[1]]})}/></label><label className="text-sm text-slate-400">装着戦法2<input list="skill-catalog" aria-label={`${roles[i]} 装着戦法2`} className={field} value={w.equippedSkills[1]} onChange={e=>updateWarrior(i,{equippedSkills:[w.equippedSkills[0],e.target.value]})}/></label></div></section>)}{error&&<p role="alert" className="text-red-400">{error}</p>}<div className="grid grid-cols-2 gap-3"><Button type="button" variant="secondary" onClick={onCancel}>キャンセル</Button><Button type="submit">保存</Button></div></form>}
