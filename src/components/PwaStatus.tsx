import {useEffect,useState} from 'react';import {applyPwaUpdate,subscribePwa,type PwaState} from '../pwa/status';import {Button} from './ui/button';
export function PwaStatus(){const [state,setState]=useState<PwaState>({offlineReady:false,updateReady:false,online:navigator.onLine});useEffect(()=>subscribePwa(setState),[]);if(state.updateReady)return <aside className="mb-3 flex items-center justify-between rounded-xl bg-blue-950 p-3 text-sm"><span>新しい版を利用できます</span><Button onClick={()=>void applyPwaUpdate()}>更新</Button></aside>;if(!state.online)return <aside role="status" className="mb-3 rounded-xl bg-slate-800 p-3 text-sm text-emerald-300">オフラインで利用中</aside>;if(state.offlineReady)return <aside role="status" className="mb-3 rounded-xl bg-slate-900 p-3 text-xs text-emerald-300">オフライン利用の準備が完了しました</aside>;return null;}
