import * as React from 'react';import {Slot} from '@radix-ui/react-slot';import {cva,type VariantProps} from 'class-variance-authority';import {cn} from '../../lib/utils';const variants=cva('inline-flex min-h-11 items-center justify-center rounded-lg px-4 font-semibold transition disabled:opacity-50',{variants:{variant:{default:'bg-amber-500 text-slate-950 hover:bg-amber-400',secondary:'bg-slate-700 text-white hover:bg-slate-600',danger:'bg-red-900 text-red-100'}},defaultVariants:{variant:'default'}});export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>,VariantProps<typeof variants>{asChild?:boolean}export function Button({className,variant,asChild=false,...props}:ButtonProps){const Comp=asChild?Slot:'button';return <Comp className={cn(variants({variant}),className)} {...props}/>;}
