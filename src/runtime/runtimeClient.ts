import {calculateRequestSchema,formalRequestSchema,searchRequestSchema,type CalculateRequest,type RuntimeOperation,type RuntimeResult} from './contracts';
type Pending={resolve:(value:RuntimeResult)=>void;reject:(reason:Error)=>void};
export class RuntimeClient{private worker:Worker|null=null;private pending=new Map<string,Pending>();private ensureWorker(){if(this.worker)return this.worker;const worker=new Worker(`${import.meta.env.BASE_URL}runtime-worker.js`);worker.onmessage=(event:MessageEvent)=>{const msg=event.data as {type:string;requestId?:string;result?:RuntimeResult;message?:string};if(!msg.requestId)return;const p=this.pending.get(msg.requestId);if(!p)return;if(msg.type==='result'){p.resolve(msg.result!);this.pending.delete(msg.requestId);}else if(msg.type==='error'){p.reject(new Error(msg.message||'runtime error'));this.pending.delete(msg.requestId);}};this.worker=worker;return worker;}private run(operation:RuntimeOperation,request:unknown){const schemas={calculate:calculateRequestSchema,search:searchRequestSchema,formal:formalRequestSchema};const payload=schemas[operation].parse(request);const requestId=crypto.randomUUID();return new Promise<RuntimeResult>((resolve,reject)=>{this.pending.set(requestId,{resolve,reject});this.ensureWorker().postMessage({type:operation,requestId,bundleUrl:`${import.meta.env.BASE_URL}runtime_bundle_b223.tgz`,request:payload});});}calculate(request:CalculateRequest){return this.run('calculate',request);}search(request:unknown){return this.run('search',request);}formal(request:unknown){return this.run('formal',request);}cancel(){this.worker?.terminate();this.worker=null;for(const p of this.pending.values())p.reject(new Error('cancelled'));this.pending.clear();}}
export const runtimeClient=new RuntimeClient();
