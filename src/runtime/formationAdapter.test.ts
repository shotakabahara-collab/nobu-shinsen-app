import {describe,expect,it} from 'vitest';import {toRuntimeFormation} from './formationAdapter';import type {Formation} from '../domain/schemas';
describe('toRuntimeFormation',()=>{it('maps existing formation JSON to calculate input without inherent skills',()=>{const now=new Date().toISOString();const w=(name:string,limitBreak:number,a:string,b:string)=>({id:crypto.randomUUID(),name,limitBreak,inherentSkill:'固有',equippedSkills:[a,b] as [string,string]});const formation={id:crypto.randomUUID(),name:'山本騎馬',kind:'ally',troopType:'騎馬',troopLevel:10,troops:10000,warriors:[w('山本勘助',2,'一行三昧','回天転運'),w('柴田勝家',1,'会盟の陣','以戦養戦'),w('柿崎景家',2,'乗勝追撃','縦横馳突')],createdAt:now,updatedAt:now} satisfies Formation;expect(toRuntimeFormation(formation)).toMatchObject({officers:['山本勘助','柴田勝家','柿崎景家'],awaken:[2,1,2],skills:['一行三昧','回天転運','会盟の陣','以戦養戦','乗勝追撃','縦横馳突']});});});
