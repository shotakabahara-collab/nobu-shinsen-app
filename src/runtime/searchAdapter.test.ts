import {describe,expect,it} from 'vitest';import type {Formation} from '../domain/schemas';import {buildOwnedSearchRequest} from './searchAdapter';
const now=new Date().toISOString(),warrior=(name:string)=>({id:crypto.randomUUID(),name,limitBreak:0,inherentSkill:'固有',equippedSkills:['A','B'] as [string,string]});const formation=(kind:'ally'|'enemy')=>({id:crypto.randomUUID(),name:kind,kind,troopType:'騎馬',troopLevel:10,troops:10000,warriors:[warrior('一'),warrior('二'),warrior('三')],createdAt:now,updatedAt:now}) satisfies Formation;
describe('buildOwnedSearchRequest',()=>it('connects owned awaken and excludes inherent skills',()=>{const base={id:crypto.randomUUID(),createdAt:now,updatedAt:now};const request=buildOwnedSearchRequest(formation('ally'),formation('enemy'),[{...base,name:'四',limitBreak:5,notes:''}],[{...base,name:'固有X',category:'固有',owned:true,description:''},{...base,id:crypto.randomUUID(),name:'装着X',category:'装着',owned:true,description:''}]);expect(request.owned_pool).toEqual([{name:'四',awaken:5}]);expect(request.skill_pool).toEqual([{name:'装着X'}]);expect(request.swap_depth).toBe(1);expect(request.skill_swap_depth).toBe(1);}));
