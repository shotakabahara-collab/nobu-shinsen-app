import {test,expect} from '@playwright/test';test('opens on iPhone and exposes transfer actions',async({page})=>{await page.goto('./');await expect(page.getByRole('heading',{name:'NOBU Companion'})).toBeVisible();await expect(page.getByRole('button',{name:/Import/})).toBeVisible();await expect(page.getByText('Battle runtime 未接続')).toBeVisible();});
