import {test,expect} from '@playwright/test';test('opens on iPhone and exposes creation, battle and transfer flows',async({page})=>{await page.goto('./');await expect(page.getByRole('heading',{name:'NOBU Companion'})).toBeVisible();await expect(page.getByRole('button',{name:'新規'})).toBeVisible();await page.getByRole('button',{name:'対戦'}).click();await expect(page.getByRole('button',{name:'10×1で計算'})).toBeDisabled();await page.getByRole('button',{name:'データ'}).click();await expect(page.getByRole('button',{name:/Import/})).toBeVisible();});
