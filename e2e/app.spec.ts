import {test,expect} from '@playwright/test';test('opens on iPhone and exposes transfer and calculate actions',async({page})=>{await page.goto('./');await expect(page.getByRole('heading',{name:'NOBU Companion'})).toBeVisible();await expect(page.getByRole('button',{name:/Import/})).toBeVisible();await expect(page.getByRole('heading',{name:'Direct Calculate'})).toBeVisible();await expect(page.getByRole('button',{name:'10×1で計算'})).toBeDisabled();});
